# Results archive

Verbatim copies of the evaluation artifacts that the numbers in the [README](../../README.md) and
[`docs/training.md`](../training.md) are taken from. They were generated by `flysweeper.validate`,
`flysweeper.mb_eval`, `flysweeper.train_mb` and `flysweeper.mb_policy --probe` in the gitignored
`outputs/` tree and copied here unchanged, except that one absolute local path in the round-2 and
round-3 validation reports was shortened to the repo-relative `data/compiled/mb_weights.npz`.
Per-game training logs (`games.jsonl`, ~0.9 MB per run) and weight checkpoints are not copied; the
three released weight sets live in [`models/`](../../models/README.md).

All games: 9×9 board, 10 mines, 15 brain steps (300 ms simulated) per turn, 400-turn limit,
dynamics preset `flyai`, synapses onto sensory neurons removed, lamina input route. Held-out seeds
are 5000–5029 (30 games) and 5000–5099 (100 games); every condition in a report plays the same
boards. "SEM" is the standard error over games.

## Held-out validation (`validation/`)

| folder | seeds | what | headline |
|---|---|---|---|
| `validation/mb_round3/` | 5000–5099 (100) | the round-3 comparison run: all conditions in one process with the round-2 weights still installed as `fly-mb` and the RL-fine-tuned round-3 candidate (`r3_rl/weights_000400.npz`, margin 0.25, mask) as `fly-mb-alt` | fly-mb-alt (RL) **76/100, 67.3 ± 1.0, 107 turns**; fly-mb (round 2) **60/100, 66.8 ± 0.9, 71 turns**; solver 86/100, 69.6; oracle-only 84/100, 68.9; frozen fly 0/100, 51.7; random-walk 0/100, 50.1; fly-blind 0/100, 49.6; random-click 0/100, 41.1 |
| `validation/mb_round3_teacher500/` | 5000–5099 (100) | the teacher-only round-3 checkpoint (`r3_teacher_l2/weights_000500.npz` = `models/mb_weights_round3.npz`, margin 0, mask) on the same seeds in a separate process, run as `fly-mb-alt` | **76/100 wins, 67.6 ± 0.9 safe cells, 66 turns** → installed. (The report header says "0 neurons, 0 edges" because `validate.py` fills that summary only when one of the standard brain conditions is in the run; `fly-mb-alt` alone loads and uses the full brain but skips the header bookkeeping — see the `mb_alt` block in `results.json` for the loaded weights.) |
| `validation/mb_round2/` | 5000–5029 (30) | all seven conditions in one run, round-2 weights installed (`models/mb_weights_round2.npz`, reveal margin 0.25) | fly-mb **18/30 wins, 67.1 ± 1.8 safe cells**; solver 27/30; oracle-only 26/30; frozen fly 0/30, 51.3; random-walk 0/30, 50.4; fly-blind 0/30, 44.5; random-click 0/30, 39.3 |
| `validation/mb_round2_100/` | 5000–5099 (100) | the round-2 release estimate and its two reference points (6 Numba threads; the same weights scored 60/100 on the same seeds with 4 threads in `mb_round3/` — a different noise stream) | fly-mb **48/100 wins, 66.7 ± 0.8**; oracle-only 84/100, 68.9; random-walk 0/100, 50.1 |
| `validation/mb_warm200/` | 5000–5029 (30) | round-1 weights (`models/mb_weights_round1.npz`) | fly-mb **0/30, 46.9 ± 2.0** (still reveals unproven cells ~12 % of the time) |
| `validation/readout/default_encoder_readoutA/` | 5000–5029 (30) | approach A: frozen brain + trained linear readout A (13,562 cells), fixed camera | fly-readout 0/30, 48.1; fly 0/30, 48.6; no readout beat random-walk |
| `validation/readout/ego_encoder_readoutE/` | 5000–5029 (30) | approach A with the egocentric camera and readout E (+T4/T5, 27,147 cells) | fly-readout 0/30, 45.9; fly 0/30, 52.6; fly-blind 0/30, 46.3 |
| `validation/readout/learning_seeds6000/` | 6000–6029 (30) | the original dopamine-gated `fly-learning` rule (`plasticity.py`) vs the frozen fly | fly-learning 0/30, 50.2 vs fly 0/30, 54.4; all 61,210 KC→MBON edges pinned at the 0.1× floor after game 1 — no learning |

Each folder holds `report.md` (the table plus the exact weight/pool state) and `results.json`
(per-game records: outcome, safe cells, turns, action counts, and the plasticity summary).

## Mushroom-body checkpoint evaluations (`mb/`)

`flysweeper.mb_eval` output: argmax agreement with the teacher on teacher-driven boards
(seeds 80000+, 600 turns unless noted), reveal recall / false-positive rate, and held-out games with
exploration off. `.md` is the table, `.json` adds the confusion matrices and per-game results.

| file | what |
|---|---|
| `mb/eval_r3_250.*` | round-3 teacher runs at 250 games (1,750 total), margin 0.25, mask: `r3_teacher_l2` (strengthened direction channels) agreement 0.732, 21/30; `r3_teacher_l1` (round-2 odor map, control) 0.717, 18/30 |
| `mb/eval_r3_ckpts.*` | `r3_teacher_l2` at 500 / 750 / 1000 games (agreement **0.763** / 0.783 / 0.770; reveal FPR 0.000 / 0.002 / 0.000; **25/30, 69.0 ± 1.3** / 22/30 / 23/30); the `r3_teacher_l1` control at 1000 (0.707, 16/30 — worse than round 2); `r3_rl` at +200 (0.714, 26/30) |
| `mb/eval_r3_rl.*` | `r3_rl` at +300 / +400 games: agreement 0.678 / 0.641, reveal FPR 0.000, 25/30 / 27/30, 104 / 100 turns per game |
| `mb/eval_r2_250.*` | the three round-2 rules at 250 games: perceptron `error` (0.607), every-turn `teacher` (0.564), `error` + sparse KC code (0.717, first held-out wins 4/30) |
| `mb/eval_r2_sparse_ckpts.*` | `r2_error_sparse` at 500 / 750 / 1000 games (agreement 0.696 / 0.691 / 0.725; 2 / 2 / 5 wins of 30) |
| `mb/eval_r2_final.*` | 1,500-game checkpoints: `r2_error_sparse` **0.735 agreement, reveal FPR 0.007, 8/30 wins at margin 0**; dense `r2_error` 0.625, 0/30; `r2_teacher` 0.586, 0/30 |
| `mb/margin_sweep_r2_sparse_1500.*` | reveal-margin sweep on **training** seeds 15000–15014 (15 games): margin 0 → 1/15, 0.10 → 5/15, 0.25 → 6/15; 0.25 was installed |
| `mb/eval_installed_agreement.*` | the installed round-2 policy (margin 0.25): agreement 0.722, reveal recall 0.952, reveal FPR 0.002 on 619 turns, with the confusion matrix |
| `mb/curriculum_r2_6x6_3.*`, `mb/curriculum_r2_6x6_4.*` | the installed round-2 policy on easier boards, seeds 5000–5029: 6×6/3 mines 24/30 wins, 6×6/4 mines 21/30 |
| `mb/probe.json` | the odor-calibration probe (`python -m flysweeper.mb_policy --probe`): PN / KC / MBON rates with the raw wiring vs the `fly-mb` KC settings, with and without odor |

## Training runs (`mb/<run>/`)

For each run: `curve.md` / `curve.json` (100-game blocks: win rate, safe cells, turns, reward,
teacher agreement, |dw|, mean weight ratio, temperature), `params.json` (the `MBParams` the run
used) and `design.json` (pools, edge counts, ORN map). Games 0–299 of `warm*` runs and all games of
the `r2_*` and `r3_teacher_*` runs are teacher-driven (the cursor follows the teacher), so their
in-run win rates are the teacher's, not the fly's; the held-out tables above are the fly's own
play. The `r3_rl` in-run win rates *are* the fly's (under ε-greedy exploration on training seeds).

| run | round | rule | games | result |
|---|---|---|---|---|
| `rl_v1` | 1 | raw KC counts, raw pool counts, reward only | 600 (stopped) | 0 wins; only pool priors moved |
| `warm_v1` | 1 | same, 300 teacher-driven games then RL | 800 (stopped) | agreement 0.18 → 0.33 during warm start; 0 wins after |
| `shuffled_v1` | 1 | `rl_v1` with the reward sign randomised (control) | ~270 (stopped) | indistinguishable from `rl_v1` |
| `rl_v2`, `warm_v2`, `shuffled_v2` | 1 | mean-subtracted KC eligibility | 500 / 250 / 350 (stopped) | `warm_v2` agreement 0.393 at 200 games; "right" never chosen |
| `warm` (v3) | 1 | + pool counts centred on their running mean; 300 warm + RL | 400 (stopped) | **agreement 0.484 at 200 games** → `models/mb_weights_round1.npz`; 0 held-out wins |
| `rl` (v3) | 1 | same, reward only | 800 | 52–54 safe cells per block, 0 wins |
| `shuffled` (v3) | 1 | same, reward sign randomised (control) | 500 | 49–52 safe cells, 1 win in 500 — same as `rl`: the round-1 reward taught nothing the control did not |
| `r2_error` | 2 | 34-type pools, floor 0, perceptron rule, dense KC code | 1,500 | agreement 0.625, 0/30 |
| `r2_teacher` | 2 | as above with the every-turn `teacher` rule | 1,500 | agreement 0.586, 0/30 |
| `r2_error_sparse` | 2 | perceptron rule + sparse KC code (PN bias −0.3, PN→KC gain 3, KC bias −0.3) | 1,500 | **agreement 0.735; 8/30 wins at margin 0, 18/30 with margin 0.25** → `models/mb_weights_round2.npz` |
| `r3_teacher_l2` | 3 | continued from the round-2 weights (seeds 14000–14999); level-2 odor map (a second ORN type per graded direction channel, 49/53 ORN types), argmax perceptron rule, move errors ×2, reveal mask, margin 0 | +1,000 (checkpoints every 250) | **checkpoint 500 (2,000 games total): agreement 0.763, 25/30 at margin 0.25, 76/100 at margin 0** → `models/mb_weights_round3.npz` |
| `r3_teacher_l1` | 3 | the same continuation with the round-2 odor map (control) | +1,000 | agreement 0.707, 16/30 at 1000 — the extra teacher games alone made it worse |
| `r3_rl` | 3 | RL fine-tune from `r3_teacher_l2` 500 with the round-3 rewards (+0.3 proven-safe reveal, −0.3 unproven reveal even if it succeeds, −1 mine, +1 win, −0.1 wasted turn), ε 0.2 → 0.02, eta 0.01, seeds 16000–16399 | +400 | training win rate 0.75–0.83 under exploration; held-out 27/30 and 76/100 but 100–107 turns per game and agreement 0.64 — not installed |

## Not archived

The round-2 curriculum runs are in `mb/curriculum_r2_6x6_*`; the round-3 policy was not run on
6×6. The reward-value analysis of round 3 (324 mid-game states; a random unproven reveal is worth
−0.07 under the round-1 reward vs −0.50 under the round-3 reward) was computed with an inline
snippet and is reported in `docs/training.md` §5 only. Training logs (`*.log`, `games.jsonl`) and
weight checkpoints other than the three in `models/` stay in the gitignored `outputs/` tree.
